import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  mapContainer: {
    height: '100%',
    width: '100%',
    backgroundColor: 'transparent',
    position: 'relative',
  },
  map: {
    flex: 1,
  },
});
